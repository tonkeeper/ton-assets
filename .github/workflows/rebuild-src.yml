name: Rebuild autogenerated files on commit
on:
  push:
    paths:
      - accounts/UQC9o3lPXSvO7ICVluBCFRF_OZHxqcNDNtIzjVItsEO6NiBN
      - collections/*
      - jettons/*
      - requirements.txt
      - generator.py
      - readme.md.template
      - .github/workflows/rebuild-src.yml
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '27 */3 * * *'
jobs:
  run-aggregator:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: Install libraries
        run: pip3 install -r requirements.txt
      - name: Run generator.py
        run: python3 generator.py
      - name: commit and push if there are changes
        run: |
          if [[ `git status --porcelain` ]]; then
            git config --global user.name "github-Mojiiiactions@"
            git config --global user.email "github-www.mmaction64@gmail.com"
            git add -A
            git commit -m 'Regenerate json and Readme'
            git push
          fi
